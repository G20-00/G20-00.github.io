---
interface Props {
  company: string;
  role: string;
  startDate: string;
  endDate: string | null;
  current: boolean;
  summary: string;
}

const { company, role, startDate, endDate, current, summary } = Astro.props;

const formatDate = (dateStr: string) => {
  const [year, month] = dateStr.split('-');
  return new Intl.DateTimeFormat('es-ES', { year: 'numeric', month: 'long' }).format(
    new Date(parseInt(year), parseInt(month) - 1)
  );
};
---

<div class="mb-4 sm:mb-8 pb-4 sm:pb-8 border-l-2 border-gray-300 dark:border-gray-700 pl-3 sm:pl-4 last:mb-0">
  <div class="flex flex-col sm:flex-row justify-between items-start gap-2 mb-1">
    <h3 class="text-xs sm:text-sm lg:text-base xl:text-lg 2xl:text-xl font-semibold text-gray-900 dark:text-white transition-all">{role}</h3>
    {current && <span class="text-xs lg:text-sm xl:text-base bg-green-100 dark:bg-green-900 text-green-800 dark:text-green-200 px-1.5 py-0.5 rounded flex-shrink-0 transition-all">Actual</span>}
  </div>
  <p class="text-xs sm:text-sm lg:text-base xl:text-lg 2xl:text-xl text-gray-600 dark:text-gray-400 font-medium mb-2 sm:mb-3 transition-all">{company}</p>
  <p class="text-xs sm:text-sm lg:text-base xl:text-lg text-gray-700 dark:text-gray-300 mb-2 sm:mb-3 leading-relaxed transition-all">{summary}</p>
  <p class="text-xs sm:text-sm lg:text-base xl:text-lg text-gray-500 dark:text-gray-400 transition-all">
    {formatDate(startDate)} — {endDate ? formatDate(endDate) : 'Presente'}
  </p>
</div>
